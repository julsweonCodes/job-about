// 근무 형태
export enum WorkType {
  Remote = "remote",
  OnSite = "on-site",
  Hybrid = "hybrid",
}

// 언어 수준
export enum LanguageLevel {
  Beginner = "Beginner",
  Intermediate = "Intermediate",
  Fluent = "Fluent",
}
